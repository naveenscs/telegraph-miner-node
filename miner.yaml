id: 901
name: "telegraph-groq-miner-node01"
description: "Groq LLaMA3 inference miner for Telegraph Protocol"
protocol: "generic"
base_url: "https://your-public-node-domain.com" # Replace with your HTTPS URL

semantics:
  supported_intents:
    - "CHAT_COMPLETION"
  label_field: "choices.0.message.content"
  confidence_field: "confidence"
  reason_field: "reason"

pricing:
  min_price_usdc: 0.01

on_chain:
  min_price_usdc: 0.01
  transform: "none"
